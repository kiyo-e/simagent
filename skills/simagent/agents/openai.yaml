interface:
  display_name: "SimAgent iOS Simulator"
  short_description: "Automate iOS Simulator via simagent CLI with stable frame-action loops"
  default_prompt: "Use $simagent to run frame-action loops on iOS Simulator with structured JSON outputs, explicit selectors, and reliable text input (`ui type --into --replace --ascii` when needed)."
